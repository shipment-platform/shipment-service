spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ime_tvoje_baze
    username: tvoje_korisnicko_ime
    password: tvoja_lozinka
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    hibernate:
      ddl-auto: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
kafka:
  bootstrap-servers: localhost:9092